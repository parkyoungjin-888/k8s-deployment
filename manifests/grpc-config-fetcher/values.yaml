namespace: default

image:
  name: img234/grpc-config-fetcher
  tag: "$IMAGE_TAG"

port: 21001

replicaCount: 1

resources:
  limits:
    cpu: "500m"
    memory: "512Mi"
  requests:
    cpu: "250m"
    memory: "256Mi"

serviceType: LoadBalancer
