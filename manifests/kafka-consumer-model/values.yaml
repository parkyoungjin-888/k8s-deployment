namespace: default

image: img234/kafka-consumer-model:0.1.1

port: 8000

appId: "kafka-consumer-model-001"

replicaCount: 1

nodeType: "server"

resources:
  limits:
    cpu: "500m"
    memory: "512Mi"
    nvidia.com/gpu: 1
  requests:
    cpu: "250m"
    memory: "256Mi"
    nvidia.com/gpu: 1

serviceType: ClusterIP
