namespace: default

image: img234/kafka-image-consumer:0.1.1

port: 8000

appId: "kafka-image-consumer-001"

replicaCount: 1

nodeType: "server"

resources:
  limits:
    cpu: "500m"
    memory: "512Mi"
  requests:
    cpu: "250m"
    memory: "256Mi"

serviceType: ClusterIP
