namespace: default

image: img234/grpc-config-fetcher:0.1.4

port: "21001"

replicaCount: 3

resources:
  limits:
    cpu: "500m"
    memory: "512Mi"
  requests:
    cpu: "250m"
    memory: "256Mi"

serviceType: LoadBalancer
