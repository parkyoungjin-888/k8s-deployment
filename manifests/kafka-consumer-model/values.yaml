namespace: default

image: img234/kafka-consumer-model:0.1.0

port: 8000

appId: "kafka-consumer-model-001"

replicaCount: 0

nodeType: "server"

resources:
  limits:
    cpu: "500m"
    memory: "512Mi"
  requests:
    cpu: "250m"
    memory: "256Mi"
#    nvidia.com/gpu: 1

serviceType: ClusterIP
